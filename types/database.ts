/**
 * Tipos do banco de dados Supabase.
 *
 * Este arquivo será substituído pelos tipos gerados automaticamente
 * (`supabase gen types typescript`) assim que as migrations forem
 * aplicadas ao projeto Supabase real (Etapa 2 — Database).
 *
 * Mantido como `any` neutro até lá para não bloquear o build,
 * sem fingir uma tipagem que ainda não existe.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
